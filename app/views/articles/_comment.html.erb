<div>
  <h5>Comment by <%= comment.author_name %></h5>
  <p>Posted <%= distance_of_time_in_words(comment.created_at, comment.article.created_at) %></p>
  <p class="comment"><%= comment.body %></p>
</div>
