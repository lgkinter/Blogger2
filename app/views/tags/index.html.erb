<h1>All Tags</h1>

<ul>
  <% @tags.each do |tag| %>
    <li><%= link_to tag.name, tag_path(tag) %>
    <%= link_to "delete", tag_path(tag), method: :delete,
    data: {confirm: "Are you sure you want to delete tag #{tag.name}?"} %></li>
  <% end %>
</ul>
